/**
 * 보존 정리의 공통 관측 — 정리가 실패하면 그 사실이 값으로 보이게 한다(ADR-058 결정 6).
 *
 * <p>정리 자체(무엇을 언제 지우는가)는 표를 가진 서비스와 모듈에 있다. 여기 있는 것은 그 모두가 같은 모양으로
 * 내는 게이지 하나뿐이다 — 이름과 라벨이 한 곳에서 나와야 알림 식 하나가 모든 표를 본다.
 */
package com.dawnline.messaging.retention;
