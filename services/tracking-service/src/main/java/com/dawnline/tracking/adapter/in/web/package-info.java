/**
 * REST 인바운드 어댑터 — 기사 스캔 이벤트 API
 * ({@code POST /api/v1/routes/{id}/stops/{seq}/events}) (DESIGN.md §5.4).
 */
@NullMarked
package com.dawnline.tracking.adapter.in.web;

import org.jspecify.annotations.NullMarked;
