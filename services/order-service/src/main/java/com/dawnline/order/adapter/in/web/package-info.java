/**
 * REST 인바운드 어댑터 — 컨트롤러·요청/응답 DTO·Bean Validation (DESIGN.md §5.1).
 *
 * <p>도메인 예외를 RFC 9457 Problem Details 로 바꾸는 단일 {@code @ControllerAdvice} 도 여기 둔다.
 */
@NullMarked
package com.dawnline.order.adapter.in.web;

import org.jspecify.annotations.NullMarked;
