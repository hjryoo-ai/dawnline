# Phase 4-17 — 희소한 것은 능력이 아니라 제약 조합이었다 (그림자 계측)

> ⚠️ **이 문서의 절대값은 `priority-boost` 의 자가 바뀌기 전의 것이다** (2026-09-12,
> [ADR-040](../adr/ADR-040-priority-boost-decays-in-time.md) — 보너스가 순번이 아니라 계획 도착
> 시각으로 감쇠한다). 재기준은 [`phase4-priority-boost.md`](phase4-priority-boost.md) §8 이고,
> 동결 `baseline-nn` 도 함께 바뀌었다(1,531,480 → **1,517,523** · 4,636,549 → **4,588,065** ·
> 9,682,478 → **9,577,578** · 28,624,728 → **28,369,930**). **이 문서의 결정은 그대로다** —
> 바뀐 것은 눈금이고, 어느 쪽이 더 싼가는 같은 실행 안의 비교라 자에 독립이다.

> `sweep-greedy-nn+ls` · `mode=FULL` · 예산 30초 · seed `20260905` · 개선 예산 계수 1.0
> 실행 환경: macOS 27.0 (aarch64) · OpenJDK 25.0.4.1 · 14 코어 · 힙 16 GB
>
> **귀속**: §1~§7 의 계획은 `f84ca35`(= `aac442b` 와 같은 최적화 코드)에서 낸 것이고, 숫자는
> 그 계획을 **읽기만 하는** 임시 프로브(`ScarceSeatProbe`)가 냈다. 프로브는 판정만 계산하고
> 동작을 바꾸지 않는다(§6.9) — 커밋하지 않았고, 만드는 법은 §6 에 적는다.
>
> **§8 은 구현 뒤의 재기준이다**(`15fa48f`, [ADR-039](../adr/ADR-039-reserve-seats-by-constraint-class.md)).
> 계측의 상한이 **아래쪽으로** 틀렸다는 것도 거기 적혀 있다.

## 0. 요약

네 문장이다.

1. **희소한 것은 「위험물」이 아니라 「냉장 ∧ 위험물」이다.** 그 조합을 싣는 차량이 실은 stop 의
   **94~100%가 일반 수요**이고, 네 데이터셋의 **미배정 위험물은 거의 전부 그 조합**이다
   (`small` 4/9 · `large` 1/1 · `peak` 9/9).
2. **능력별로 재면 아무것도 안 보인다.** `peak` 에서 위험물 차량의 남은 슬롯은 287개인데,
   그 조합을 싣는 9대의 남은 슬롯은 **5개**다. 「위험물 수요 284 ≤ 여유 287」로 읽으면
   「좌석은 충분하다」가 나온다 — 그리고 9건이 미배정으로 남는다.
3. **상한만 재면 [ADR-038](../adr/ADR-038-fixed-cost-floor-is-not-a-total-cost-floor.md) 의 실수를
   반복한다.** 미배정 위험물 페널티(= 엄밀한 상한)는 350,000 / 0 / 30,000 / 290,000원인데,
   그 좌석을 실제로 만들어 앉히면 `large` 는 **−10,945원**이다. 싣는 것이 페널티보다 비싸다.
4. **처방은 「배정 순서」가 아니라 「좌석 예약」이고, 예약의 단위는 제약 조합이다.** 네
   데이터셋에서 **클러스터의 100%가 위험물 stop 을 포함**하므로 「희소 수요를 요구하는 클러스터를
   먼저」는 완전한 no-op 이다.

---

## 1. ⓐ 좌석 점유 — 조합 차량의 좌석은 94~100%가 일반 수요다

계획이 끝난 뒤의 라우트를 읽는다. 「요구」는 그 능력을 *실제로 필요로 하는* stop 이다.

**위험물 차량** (함대의 20%)

| | 차량 | 실은 stop | 위험물 요구 | **일반** | 일반 비율 | 남은 슬롯 |
|---|---:|---:|---:|---:|---:|---:|
| `small` | 1 | 120 | 3 | 117 | **98%** | **0** |
| `medium` | 4 | 313 | 34 | 279 | 89% | 167 |
| `large` | 8 | 836 | 85 | 751 | 90% | 124 |
| `peak` | 18 | 1,873 | 275 | 1,598 | 85% | 287 |

**냉장 ∧ 위험물 차량** — 같은 표를 조합으로 다시 낸다

| | 차량 | 실은 stop | 조합 요구 | **일반** | 일반 비율 | 남은 슬롯 | 조합 수요 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `small` | 1 | 120 | **0** | 120 | **100%** | **0** | 4 |
| `medium` | 2 | 230 | 11 | 219 | 95% | 10 | 11 |
| `large` | 4 | 464 | 22 | 442 | 95% | 16 | 23 |
| `peak` | 9 | 1,075 | 68 | 1,007 | **94%** | **5** | 77 |

`small` 의 한 줄이 이 항목 전체다. **그 조합을 실을 수 있는 차는 1대뿐인데, 그 차의 120 자리가
전부 일반 수요로 찼다** — 조합 수요 4건은 한 자리도 받지 못하고 전부 미배정이다.

**미배정의 정체**

| | 미배정 stop | 위험물 | 그중 **냉장도** 요구 | 페널티 | 거절 사유(계획이 남긴 것) |
|---|---:|---:|---:|---:|---|
| `small` | 9 | 9 | **4** | 350,000 | `hazmat` 5 · `cold-chain` 4 |
| `medium` | 0 | 0 | 0 | 0 | — |
| `large` | 1 | 1 | **1** | 30,000 | `hazmat` 1 |
| `peak` | 10 | 9 | **9** | 290,000 | `hazmat` 9 · `late-hard-limit` 1 |

`peak` 의 9건을 18대의 위험물 차량에 하나씩 대 보면 거절이 **`max-stops` 72 · `cold-chain` 81 ·
`shift-window` 9** 로 갈린다 — `cold-chain` 81 = 9건 × 비냉장 위험물 차 9대다. **9건 전부가 냉장을
함께 요구**하고, 그것을 실을 수 있는 9대는 이미 `max-stops` 에 닿아 있다.

---

## 2. 회수 상한, 그리고 그 상한을 실제로 걷었을 때

[ADR-038](../adr/ADR-038-fixed-cost-floor-is-not-a-total-cost-floor.md) 이 남긴 교훈이 여기 적용된다
— **상한은 잰 항이 맞을 때만 뜻이 있다.** 그래서 두 값을 함께 낸다.

- **상한(엄밀)**: 미배정 위험물의 페널티 총액. 좌석으로 회수할 수 있는 양은 이보다 클 수 없다.
- **실현(한 번의 재배치)**: 좌석을 실제로 만들어 앉힌다 — 꼬리의 일반 stop 을 필요한 만큼만
  내리고, **모든 자리를 넣어 보며 라우트 전체를 다시 검증**해 앉힌 뒤, 내린 stop 을 다른 차가
  받게 한다. 여기서 드는 비용을 전부 뺀다.

| | 상한 | 앉히는 비용 | 이동 비용 | **실현** | 내린 일반 stop | −15%까지 격차 |
|---|---:|---:|---:|---:|---:|---:|
| `small` | 350,000 | 81,041 | 51,065 | **217,894** | 15 | **188,755** |
| `medium` | 0 | — | — | **0** | 0 | 이미 −15.47% |
| `large` | 30,000 | 40,945 | 0 | **−10,945** | 0 | 46,024 |
| `peak` | 290,000 | 182,034 | 69,238 | **38,728** | 9 | — |

세 가지가 읽힌다.

1. **`small` 은 이 항목으로 닫힌다.** 실현 217,894원이 격차 188,755원보다 크다. 좌석을 비워야 할
   일반 stop 은 15개이고 **15개 전부 다른 차가 받았다**(비위험물 차량의 여유 슬롯 139).
2. **`large` 는 이 항목의 대상이 아니다.** 유일한 미배정 1건은 **지금 상태로도 들어간다** —
   들어가지 않은 이유는 좌석이 없어서가 아니라 **한계비용 36,653원 > 페널티 30,000원**이기
   때문이다. §6.1 의 목적함수가 「싣지 않는 것이 싸다」고 옳게 판단한 자리다. 상한만 봤다면
   여기서도 30,000원을 벌 수 있다고 읽었을 것이다.
3. **`medium` 은 미배정이 없다.** 좌석이 물지 않는다.

> 근사의 방향을 적는다. **회수**는 페널티 전액이라 정확하고, **앉히는 비용**은 최선 자리를
> 찾되 국소 탐색을 다시 돌리지 않으므로 과대평가 방향, **이동 비용**은 끝에 붙이기라 역시
> 과대평가 방향이다. 그래서 「실현」 열은 **달성 가능한 값의 아래쪽**이고, 「상한」 열이 위쪽이다.
> 두 열 사이가 이 항목이 움직일 수 있는 폭이다.

---

## 3. ⓑ 배정 순서인가 좌석 예약인가 — 순서는 no-op 이다

**클러스터의 100%가 위험물 stop 을 포함한다.**

| | 클러스터 | 위험물 포함 | 냉장 포함 | 클러스터당 위험물 stop (최소·중앙·최대) |
|---|---:|---:|---:|---|
| `small` | 2 | **2 (100%)** | 2 (100%) | 5 · 7 · 7 |
| `medium` | 6 | **6 (100%)** | 6 (100%) | 2 · 6 · 9 |
| `large` | 14 | **14 (100%)** | 14 (100%) | 3 · 7 · 10 |
| `peak` | 42 | **42 (100%)** | 42 (100%) | 2 · 7 · 14 |

위험물은 주문의 2%지만 클러스터 하나가 61~120 stop 이므로 **모든 클러스터가 그 부류**가 된다.
「희소 능력을 요구하는 클러스터를 먼저 배정」은 **아무 순서도 바꾸지 않는다.**

**예약은 능력이 아니라 제약 조합에 건다.** 능력별로 재면 이렇게 보인다:

| `peak` | 수요 | 그 차량의 남은 슬롯 | 능력별로 읽으면 |
|---|---:|---:|---|
| 위험물 | 284 | 287 | 「충분하다 — 예약 불필요」 |
| **냉장 ∧ 위험물** | 77 | **5** | 「9건이 갈 곳이 없다」 |

능력별 예약은 `peak` 에서 **아무것도 하지 않는다.** 축은 [ADR-033](../adr/ADR-033-constraint-classes.md)
이 이미 세워 둔 **제약 조합**이고, 이 항목은 그 축을 배정에 처음으로 쓰는 자리다.

예약의 대가도 작다 — 조합 수요는 조합 차량 용량의 일부일 뿐이다(`peak` 77/1,080 슬롯).
`medium`·`large` 는 조합 수요가 이미 전부 앉아 있어 **예약해도 내려오는 일반 stop 이 없다**
(0개). 값을 무는 것은 `small`(15개)과 `peak`(9개)이고, 둘 다 다른 차가 전부 받았다.

---

## 4. ⓒ `leftover` 첫째 키를 둔 채로도 걷히는가 — 걷힌다

키 순서는 「넣지 못한 stop 수 → 한계비용 → 능력 순위 → 용량 → 차량 id」다(§6.5 3단계).
모든 클러스터가 위험물과 냉장을 함께 포함하므로, **조합 차량은 언제나 `leftover` 가 가장 작다** —
첫째 키가 구조적으로 **가장 희소한 자원을 가리킨다.** 위험물 차량 중 위험물 stop 을 한 개도 싣지
않은 차량은 **0대**(네 데이터셋 전부)인 것이 그 압력의 흔적이다.

그렇지만 **좌석 예약은 하드 용량이라 그 위에서 성립한다.** 키가 그 차를 계속 가리켜도, 예약된
좌석은 일반 수요에 열리지 않는다. 그래서 **4-17 은 키 순서를 바꾸지 않고 할 수 있다.**

키 순서 자체의 문제(트럭이 stop 의 59%를 싣는다)는 남지만, 그것은 4-8·4-18 의 몫이다
([측정](phase4-cluster-axis.md) §4.1). **한 커밋에 섞지 않는다.**

---

## 5. 결정과 제안

- **처방**: 제약 조합별 좌석 예약. 배정 순서는 이 데이터에서 no-op 이므로 버린다.
- **예상 값어치**: `small` 217,894원(격차 188,755원을 넘는다) · `medium` 0 · `large` −10,945원 ·
  `peak` 38,728원. **즉 이 항목 단독의 이득은 `small` 에 있다.**
- **그런데 `large` 에서의 값어치는 조건부다.** 지금의 40대 상태에서는 좌석이 물지 않지만,
  고정비를 줄이는 변형(4-8 의 축 정정, [ADR-038](../adr/ADR-038-fixed-cost-floor-is-not-a-total-cost-floor.md) §3 의 네 변형)에서는
  **늘어난 미배정이 전부 위험물**이었다. 좌석은 차가 줄어야 무는 것이고, 그래서 두 항목이
  얽혀 있다 — 이 계측이 그 얽힘을 수치로 확인했다.
- ~~**[결정 필요]**~~ → **승인·구현 완료** (2026-09-12,
  [ADR-039](../adr/ADR-039-reserve-seats-by-constraint-class.md)). 순서는 그대로 17 → 8 이고,
  4-8 의 네 조합은 그 위에서 다시 낸다. 결과는 **§8**: `large` 는 예상대로 움직이지 않았고
  (+5,516원), 나머지 셋은 **예상보다 크게** 움직였다 — 상한을 좁게 잰 탓이다.

## 6. 프로브를 다시 만드는 법

`tools/benchmark` 에 `ScarceSeatProbe` 를 두고 `BenchmarkMain` 이 환경변수 하나로 그것을 부른다.
프로브가 하는 일은 넷이다.

1. 계획 결과의 라우트를 차량 속성별로 갈라 「실은 stop / 능력을 요구하는 stop / 남은 슬롯」을 센다.
2. 미배정을 `StopMerger` 결과와 맞춰 제약별로 갈라 페널티를 더한다.
3. 좌석을 만든다 — 꼬리의 일반 stop 을 하나씩 내리며 **모든 자리에 넣어 보고 라우트 전체를
   다시 검증**한다(`RouteState` 를 처음부터 다시 쌓으며 `RuleSet.check`). 앉으면 그 차의
   라우트 비용 차이를 더하고, 내린 stop 은 다른 차에 끝 붙이기로 옮겨 그 비용도 더한다.
4. 클러스터러를 같은 파라미터로 다시 돌려(`SweepClusterer(8)`) 클러스터별 희소 수요 포함 여부를 센다.

## 7. 재검토 지점

- **위험물 비율이 2%보다 커지는 날.** 클러스터 100%가 위험물을 포함하는 것은 「2% × 큰 클러스터」의
  결과다. 비율이 오르면 「순서」 처방이 다시 뜻을 가질 수 있다.
- **조합이 셋 이상 겹치는 날**(냉장 ∧ 위험물 ∧ 무엇). 예약을 조합별로 걸면 조합 수가 늘 때
  예약이 함대를 조각낸다 — 그때는 조합을 열거하지 말고 수요에서 유도해야 한다.
- **4-8 이 들어가는 날.** 차량이 줄면 좌석이 물기 시작하고, 이 문서의 「실현」 열이 전부 바뀐다.

---

## 8. 재기준 — 좌석 예약을 넣고 다시 잰 다섯 열 (2026-09-12)

> `mode=FULL` · 예산 30초 · seed `20260905` · 개선 예산 계수 1.0 · 전략당 **1회**(결정적) ·
> 전부 **수렴 종료**. 실행 환경은 이 문서 머리말과 같다.
>
> **귀속**: 「전」은 `a533c95`, 「후」는 `15fa48f`([ADR-039](../adr/ADR-039-reserve-seats-by-constraint-class.md)).
> 두 열 모두 **이 기계에서 다시 냈다** — 다른 리포트의 절대 수치를 옮겨 적지 않았다(§6.9).
> `baseline-nn` 은 동결이고 **한 자리도 바뀌지 않았다**(세 값이 전후 동일).

### 8.1 총비용 — `sweep-greedy-nn+ls`

| 데이터셋 | `baseline-nn`(동결) | 전 | **후** | 변화 |
|---|---:|---:|---:|---:|
| `small` | 1,531,480 | 1,490,513 (−2.67%) | **1,136,026 (−25.82%)** | **−354,487** |
| `medium` | 4,636,549 | 3,919,106 (−15.47%) | **3,893,515 (−16.03%)** | −25,591 |
| `large` | 9,682,478 | 8,276,130 (−14.52%) | **8,281,646 (−14.47%)** | **+5,516** |
| `peak` | 28,624,728 | 22,341,428 (−21.95%) | **21,774,900 (−23.93%)** | −566,528 |

**미배정**

| 데이터셋 | 전 | 후 | 페널티 전 → 후 |
|---|---:|---:|---|
| `small` | 9 (전부 위험물) | **0** | 350,000 → **0** |
| `medium` | 0 | 0 | 0 → 0 |
| `large` | 1 (위험물) | **0** | 30,000 → **0** |
| `peak` | 10 (위험물 9) | **1** | 320,000 → **30,000** |

**개선 단계 없이도 바뀐다** (`sweep-greedy-nn`, 5회 · p50)

| 데이터셋 | 전 | 후 | 미배정 전 → 후 |
|---|---:|---:|---|
| `small` | 1,624,788 (**+6.09%**) | **1,287,456 (−15.93%)** | 9 → **0** |
| `medium` | 4,290,728 (−7.46%) | 4,315,281 (−6.93%) | 1 → **0** |
| `large` | 9,053,096 (−6.50%) | 9,056,167 (−6.47%) | 4 → 4 |

`small` 의 한 줄이 이 항목의 성격을 말한다. **클러스터링 단독이 베이스라인에 지던 것이 뒤집혔다** —
그동안 그 격차를 닫은 것은 개선 단계였는데(README 의 「레짐 격차」 문단), 실제로 지고 있던 이유는
기하가 아니라 **좌석**이었다.

### 8.2 `large` 는 어디서 졌나 — 같은 항이다

| 항 | 전 | 후 | 차이 |
|---|---:|---:|---:|
| 고정비 | 2,500,000 | 2,500,000 | 0 |
| 거리비 | 1,310,622 | 1,304,365 | −6,257 |
| 시간비 | 4,505,510 | 4,500,747 | −4,763 |
| **소프트** | **−70,002** | **−23,466** | **+46,536** |
| 미배정 | 30,000 | 0 | −30,000 |
| **합** | 8,276,130 | 8,281,646 | **+5,516** |

소프트를 룰별로 가르면:

| 룰 | 전 | 후 | 차이 |
|---|---:|---:|---:|
| `priority-boost` | −382,202 (477건) | −350,866 (477건) | **+31,336** |
| `zone-affinity` | 214,000 (107건) | 222,000 (111건) | +8,000 |
| `late-penalty` | 18,200 (26건) | 25,400 (24건) | +7,200 |
| `vehicle-preference` | 80,000 (20건) | 80,000 (20건) | 0 |

**4-8 의 축 정정이 `large` 에서 졌던 것과 같은 항이다**(그때 +83,923원, [측정](phase4-cluster-axis.md) §4).
`bonusKrw × priority ÷ position` 은 라우트가 길어질수록 보너스가 줄어, 구조적으로 **차를 더 쓰는
계획**에 유리하다. 두 항목이 연속으로 같은 자에 진 것이 **4-18** 의 근거를 한 번 더 준다 —
그리고 그 자를 지금 고치지 않는 판단은 그대로다(재는 자를 고치는 일이다).

반대로 `peak` 은 같은 항에서 이겼다: 지각 stop 182 → **113**, `late-penalty` 202,050 → **79,700원**.

### 8.3 상한이 이번에는 **아래쪽**으로 틀렸다

| 데이터셋 | 상한(§2 의 미배정 페널티) | §2 의 「실현」 | **실제** | 폭 안인가 |
|---|---:|---:|---:|---|
| `small` | 350,000 | 217,894 | **354,487** | **넘었다** |
| `medium` | 0 | 0 | **25,591** | **넘었다** |
| `large` | 30,000 | −10,945 | **−5,516** | 안 |
| `peak` | 290,000 | 38,728 | **566,528** | **두 배** |

§2 의 근사 방향 설명은 「실현은 아래쪽, 상한은 위쪽」이었고 **그 안쪽은 맞았다** — 실제가
실현보다 크다. 틀린 것은 **상한 쪽**이다.

> **상한은 「그 항」의 상한이지 「그 변경」의 상한이 아니다.** 여기서 잰 항은 「미배정 페널티의
> 회수」였는데, 좌석 예약은 미배정만 걷지 않고 **배정 자체를 바꾼다**. [ADR-038](../adr/ADR-038-fixed-cost-floor-is-not-a-total-cost-floor.md)
> 이 「잰 항이 틀렸다」를 위쪽으로 배웠다면 여기서는 아래쪽으로 배운다 — 어느 쪽으로 틀렸는지는
> 부호가 아니라 **항의 범위**가 정한다. 그래서 상한을 낼 때는 **그 변경이 건드리는 항을 먼저
> 적는다**(§6.9 에 보강).

그렇다고 §2 가 헛일이었던 것은 아니다. **`large` 만은 폭 안에 들어왔고**, 그 한 줄이 「4-17
단독으로는 `large` 의 DoD 가 움직이지 않는다」를 미리 말해 줬다 — 실제로 +5,516원이다.

### 8.4 두 변경을 따로 재면

한 커밋에 둘이 들어 있다: **좌석 예약**과 **재삽입 동률의 둘째 키**(앉을 자리가 적은 수요부터).
따로 잰다.

| 조합 | `small` | `medium` | `large` | `peak` |
|---|---|---|---|---|
| 전 | 1,490,513 (9) | 3,919,106 (0) | 8,276,130 (1) | 22,341,428 (10) |
| **동률 키만** | 1,490,513 (9) | 3,919,106 (0) | **8,257,720 (0)** | **22,619,366 (19)** |
| **예약만** | 1,136,026 (0) | 3,893,515 (0) | 8,281,646 (0) | **21,731,782 (0)** |
| **둘 다**(들어간 것) | 1,136,026 (0) | 3,893,515 (0) | 8,281,646 (0) | 21,774,900 (1) |

괄호는 미배정 수다. **동률 키는 단독으로는 나쁘다** — `peak` 에서 미배정이 10 → 19 로 늘고
277,938원이 오른다. 예약과 함께여도 `peak` 에서 43,118원을 문다(미배정 0 → 1).

그런데도 넣는다. 이유는 수치가 아니다 — **예약이 뜻을 가지려면 그 좌석을 그 수요가 먼저 봐야
하기 때문**이고, 정하지 않으면 그 순서를 **주문 UUID 가 정한다**(페널티가 같을 때 재삽입의
마지막 키다). 그것을 재는 것은 데이터셋이 아니라 테스트다:
`ScarceSeatPlanTest.조합_차량의_자리는_그_조합의_수요에_남는다` — 자리가 정확히 18개(상한 6 ×
3대)라 한 자리도 남지 않는 계획에서, 이 키가 없으면 일반 수요가 예약 좌석을 먼저 가져가 조합
수요 한 건이 미배정으로 남는다. 네 데이터셋에는 여유가 있어 그 상황이 나타나지 않을 뿐이다.

**재검토 지점**: 여유가 없는 데이터셋(`overload`)이나 조합 비율이 오르는 날에는 이 키의 부호가
바뀔 수 있다. 그때는 순서가 아니라 **예약을 재삽입의 첫 패스까지 유지하는 안**을 다시 본다.

### 8.5 옆으로 움직인 것 — 열화 사다리의 첫 단이 더 이상 물지 않는다

좌석 예약이 **개선 단계의 일을 줄였다**. 배정이 더 나은 자리에서 시작하니 국소 탐색이 일찍
수렴한다.

| `large` | 전 | 후 |
|---|---|---|
| FULL | 8,276,130 · p95 5,680 ms | 8,281,646 · p95 **4,574 ms** |
| 개선 예산 ×0.5 | +1.35% · p95 3,208 ms | **+0.00%** · p95 4,750 ms (수렴) |
| FAST (5단계 생략) | +9.39% · p95 1,542 ms | **+9.35%** · p95 1,747 ms |

[ADR-034](../adr/ADR-034-degrade-mode.md) 의 사다리 첫 단(예산 절반)은 `large` 에서 **대가가
0이 됐다** — 개선이 절반 예산 안에 수렴하기 때문이다. 사다리의 근거(두 조건은 다른 것을 뜻한다)는
그대로지만 **첫 단의 수치는 다시 재야 한다**. 4-5(벤치마크 리포트)에서 전 데이터셋으로 낸다.

### 8.6 DoD

| | 목표 | 현재 | |
|---|---|---|---|
| `small` | ≥ 15% | **−25.82%** | ✅ (이 항목이 닫았다) |
| `medium` | ≥ 15% | **−16.03%** | ✅ |
| `large` | ≥ 15% | **−14.47%** | ❌ **51,540원 부족** |
| `peak` | — | −23.93% | 계획 p95 18.6초 < 30초 |

`large` 의 남은 51,540원은 4-8(클러스터 축)과 4-18(`÷ position`)이 가리키는 자리에 있다. 4-8 의
네 조합은 **이 결정 위에서** 다시 잰다 — 그 표의 A 행은 이제 8,281,646 이다.
